# 테스트 환경(설정을 따로 가져가는게 맞다[운용과 목적이 다르기 때문])
# In-Memory H2(Memory DB for Test) https://www.h2database.com/html/cheatSheet.html [외부 접속 안해도됨 SpringBoot]
spring:
#  datasource:
#    url: jdbc:h2:mem:test -> 별도의 설정이 없으면 In-Memoery로 돌린다
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      show_sql: true
#      ddl-auto: create-drop -> 종료 시점 drop쿼리 날린다(자원 정리)
#    properties:
#      hibernate.format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace